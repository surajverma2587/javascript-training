/**
 * Write a function that accepts an object and
 * a key to search for. If the key is present
 * in the object then the function should return
 * "Key found" and if the key is not present then
 * the function should return "Key not found"
 * 
 * NOTE: use hasOwnProperty method
 */

const hasKeyInObject = () => {}

export default hasKeyInObject