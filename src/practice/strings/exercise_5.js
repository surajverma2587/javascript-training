/**
 * Write a tagged template function called upper
 * which converts all the values in a template literal
 * to the uppercase value and returns the string
 * 
 * NOTE: use tagged template literals
 */

const upper = () => {}

export default upper