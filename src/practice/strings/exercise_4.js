import { oneLineTrim } from 'common-tags'

/**
 * Write a function that accepts a multi-line 
 * string as the input and returns a single-line
 * string as the output.
 * 
 * Use the tagged template 'oneLineTrim' which
 * converts a multi-line string to a single-line
 * 
 * NOTE: use tagged template literals
 */

const convertToSingleLine = () => {}

export default convertToSingleLine