/**
 * Write a function that returns a string:
 * If the age is below 18 then return 
 * 'Challenge for <age> years - REQUIRED'
 * If the age is 18 and above then return 
 * 'Challenge for <age> years - NOT REQUIRED'
 * 
 * Call another function within the template literal
 * which will return REQUIRED if the above condition is true
 * and NOT REQUIRED if the above condition is false
 * 
 * NOTE: use template literals
 */

const challengeRequired = () => {}

const legalAgeChallenger = () => {}

export default legalAgeChallenger