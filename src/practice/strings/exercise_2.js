/**
 * Write a function that returns a formatted age as 
 * "My age is 30." where age is the sum of the two
 * arguments passed in to the function
 * 
 * NOTE: use template literals
 */

const ageFormatter = () => {}

export default ageFormatter
